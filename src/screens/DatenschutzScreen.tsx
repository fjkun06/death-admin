import { classNameGenerator } from "@/utils";
import React from "react";

const DatenschutzScreen = () => {
  return (
    <main className={classNameGenerator("datenschutz")}>DatenschutzScreen</main>
  );
};

export default DatenschutzScreen;
